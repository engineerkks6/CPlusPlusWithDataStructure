#include <iostream>
#include <math.h>
using namespace std;

int main()
{

    // int n;
    // cin >> n;

    // int reverse = 0;
    // while (n > 0)
    // {
    //     int lastdigit = n % 10;
    //     reverse = reverse * 10 + lastdigit;
    //     n = n / 10;
    // }

    // cout << "reverse = " << reverse << endl;

    // return 0;
    // ArmStrong No ----------------------------------------------
    // int n;
    // cin >> n;

    // int sum = 0;
    // int originalN = n;
    // while (n > 0)
    // {
    //     int lastdigit = n % 10;
    //     sum += pow(lastdigit, 3);
    //     n = n / 10;
    // }

    // if (sum == originalN)
    //     cout << "Armstrong no ";
    // else
    //     cout << "Not Armstrong No ";

    // Prime No in optimized solution----------------------------------------------

    // int n;
    // cin >> n;

    // bool flag = 0;

    // for (int i = 2; i <= sqrt(n); i++)
    // {
    //     if (n % i == 0)
    //     {
    //         cout << "Not A Prime No ";
    //         flag = 1;
    //         break;
    //     }
    // }

    // if (!flag)
    //     cout << " Prime No ";

    // int n;
    // cin >> n;

    // int fact = 1;
    // for (int i = 2; i <= n; i++)
    // {
    //     fact = i * fact;
    // }

    // cout << fact;
}